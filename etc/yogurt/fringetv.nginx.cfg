upstream fringeApp {
	 server localhost:8080;
	 server localhost:8081;
	 server localhost:8082;
}

server {
	listen 80 default_server;
	listen [::]:80 default_server ipv6only=on;

	server_name .fringetv.org;

	location / {
		 proxy_pass  http://fringeApp;
        }
}
